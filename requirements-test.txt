# Gate 1 exercises the numerical and TSV-loading modules without the UI stack.
numpy

# Pandas 3 makes the production loader's in-place percentage normalization fail
# on read-only data, so the compatibility baseline remains on pre-3 behavior.
pandas<3
pytest>=8
